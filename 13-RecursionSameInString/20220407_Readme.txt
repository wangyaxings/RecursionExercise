总结：
（1）这道题的递归退出条件并不唯一；
（2）对数组操作的好像很多都是传入三个参数，一个是被处理的对象本身，另外两个一个是start，一个是end，并且start不断靠近end
退出条件也大同小异，唯一区别较大的是递归前或后的处理不同；
（3）可以利用默认参数，但是起始值=0的默认值只能放在最后一个变量；
（4）又考虑过line-41（//SameInString(x, start + 1, end);），如果不写return为什么会报错的原因，想着总能从
line-28和line-37这两行其中一行退出，不明白为什么报错，仔细一想，它和while循环之类的break或return并不相同，递归最后退出也只能将结果不断向外返回，
直到最外层处，如果没有return还是有问题的；